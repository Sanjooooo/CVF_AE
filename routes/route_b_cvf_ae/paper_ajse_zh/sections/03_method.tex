\section{CVF-AE 方法}
\label{sec:method}

CVF-AE 不以约束可行性场（Constraint Viability Field, CVF）替代基础自适应进化搜索，而将其置于候选生成阶段：障碍物、禁飞区、高度、转弯、边界和风险信息先构造为有界方向增量，再仅作用于少量关键候选，使约束违反信息在评价前提供局部移动建议，同时保留基础搜索的全局探索职责。

\subsection{基础 AE 与 CVF-AE 流程}

基础 AE 与 CVF-AE 共享第 \ref{sec:problem-formulation} 节中的路径编码、目标函数、违反分数、边界投影和 Deb 规则。基础 AE 使用固定的平衡进化算子；CVF-AE 仅在稀疏触发时生成额外的约束引导候选，并采用更严格的局部门控。

\begin{algorithm}[H]
\caption{基础 AE 路径搜索流程}
\label{alg:base-ae}
\scriptsize
\begin{algorithmic}[1]
\Require 地图 $\mathcal{M}$，起点 $\mathbf{s}$，终点 $\mathbf{g}$，种群规模 $N$，最大迭代次数 $T$
    \Ensure 按 Deb 规则得到的当前最优路径 $\mathcal{P}^*$
\State 初始化种群 $\{\mathbf{X}_i\}_{i=1}^{N}$，并将控制点限制在边界内
\State 评价所有个体的 $J(\mathbf{X}_i)$ 与 $V(\mathbf{X}_i)$
\State 按 Deb 可行性优先规则记录当前最优解 $\mathbf{X}^*$
\For{$t=1$ to $T$}
    \For{$i=1$ to $N$}
        \State 构造固定基础进化方向 $\mathbf{D}_{\mathrm{AE}}$
        \State 按式 \eqref{eq:ae-candidate} 生成基础候选 $\mathbf{X}_{\mathrm{AE}}$
        \State 评价 $\mathbf{X}_{\mathrm{AE}}$ 的目标值和违反量
        \If{$\mathbf{X}_{\mathrm{AE}} \prec_{\mathrm{Deb}} \mathbf{X}_i$}
            \State $\mathbf{X}_i\gets \mathbf{X}_{\mathrm{AE}}$
        \EndIf
        \State 更新当前最优解 $\mathbf{X}^*$
    \EndFor
\EndFor
\State \Return 由 $\mathbf{X}^*$ 解码得到的路径 $\mathcal{P}^*$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[H]
\caption{CVF-AE 路径搜索流程}
\label{alg:cvf-ae}
\scriptsize
\begin{algorithmic}[1]
\Require 地图 $\mathcal{M}$，起点 $\mathbf{s}$，终点 $\mathbf{g}$，种群规模 $N$，最大迭代次数 $T$
    \Ensure 按 Deb 规则得到的当前最优路径 $\mathcal{P}^*$
\State 生成参考控制序列，并进行约束感知初始化
\State 评价所有个体的 $J(\mathbf{X}_i)$ 与 $V(\mathbf{X}_i)$
\State 按 Deb 可行性优先规则记录当前最优解 $\mathbf{X}^*$
\For{$t=1$ to $T$}
    \State 识别观测状态 $s_{\mathrm{obs}}$，并通过保守调度得到生效状态 $s_t$
    \For{$i=1$ to $N$}
        \State 按生效状态构造内部基础候选 $\mathbf{X}_{\mathrm{AE}}$
        \State 评价 $\mathbf{X}_{\mathrm{AE}}$ 的目标值和违反量
        \If{个体 $i$ 满足 CVF 稀疏触发条件}
            \State 构造 $\mathbf{D}_{\mathrm{CVF}}$ 与 $\mathbf{D}_{\mathrm{Q}}$
            \State 按式 \eqref{eq:cvf-candidate} 生成 $\mathbf{X}_{\mathrm{CVF}}$
            \State 评价 $\mathbf{X}_{\mathrm{CVF}}$ 的目标值和违反量
            \If{$\mathbf{X}_{\mathrm{CVF}} \prec_{\mathrm{Deb}} \mathbf{X}_{\mathrm{AE}}$ 且 $J(\mathbf{X}_{\mathrm{CVF}})\leq J(\mathbf{X}_{\mathrm{AE}})$}
                \State $\mathbf{Y}\gets \mathbf{X}_{\mathrm{CVF}}$
            \Else
                \State $\mathbf{Y}\gets \mathbf{X}_{\mathrm{AE}}$
            \EndIf
        \Else
            \State $\mathbf{Y}\gets \mathbf{X}_{\mathrm{AE}}$
        \EndIf
        \If{$\mathbf{Y}$ 被选入稀疏可行性保持集合}
            \State 对 $\mathbf{Y}$ 进行局部路径修复并重新评价
        \EndIf
        \If{$\mathbf{Y} \prec_{\mathrm{Deb}} \mathbf{X}_i$}
            \State $\mathbf{X}_i\gets \mathbf{Y}$
        \EndIf
        \State 更新当前最优解 $\mathbf{X}^*$
    \EndFor
\EndFor
\State \Return 由 $\mathbf{X}^*$ 解码得到的路径 $\mathcal{P}^*$
\end{algorithmic}
\end{algorithm}
% \FloatBarrier

基础 AE 的候选由固定平衡进化方向、凸组合和边界投影构成：
\begin{equation}
    \mathbf{X}_{\mathrm{AE}} =
    \Pi_{\Omega}
    \left(
    (1-\eta_t-\mu_t)(\mathbf{X}_i+\mathbf{D}_{\mathrm{AE}})
    +\eta_t\mathbf{X}_i+\mu_t\mathbf{X}_{\mathrm{elite}}
    \right)
    \label{eq:ae-candidate}
\end{equation}
其中 $\Pi_{\Omega}(\cdot)$ 为边界投影，$\mathbf{X}_{\mathrm{elite}}$ 为精英池采样个体。令 $\tau=t/T$，$\mathbf d_{\mathrm{best}}=\mathbf X^*-\mathbf X_i$，$\mathbf d_{\mathrm{elite}}=\bar{\mathbf X}_{\mathrm{elite}}-\mathbf X_i$，$\mathbf d_{\mathrm{sample}}=\mathbf X_{\mathrm{elite}}-\mathbf X_i$，$\mathbf d_{ab}$、$\mathbf d_{cd}$ 为随机差分向量，$\boldsymbol\xi\sim\mathcal U([-1,1]^{3K})$。其方向为
\begin{equation}
    \begin{aligned}
    \mathbf D_{\mathrm{AE}}
    ={}&a_e\mathbf r_e\odot\mathbf d_{\mathrm{elite}}
    +a_b\mathbf r_b\odot\mathbf d_{\mathrm{best}}\\
    &+a_d\bigl(\omega_d\mathbf d_{ab}+(1-\omega_d)\mathbf d_{cd}\bigr)
    +a_s\mathbf r_s\odot\mathbf d_{\mathrm{sample}}\\
    &+a_u\rho_t\boldsymbol\xi\odot(\mathbf u-\mathbf l)
    \end{aligned}
\label{eq:ae-base-direction}
\end{equation}
其中各 $\mathbf r$ 为 $[0,1]^{3K}$ 上的独立随机向量，$\odot$ 表示逐维乘积；$a_e$、$a_b$、$a_d$、$a_s$ 和 $a_u$ 为平衡精英、最优、差分、采样与随机扰动的固定调度系数，$\omega_d$ 为差分组合权重，$\eta_t$ 和 $\mu_t$ 为随进程平滑变化的凸组合系数。上述设置在所有场景中保持一致。

\subsection{CVF-AE 算法设计}

CVF-AE 的新增环节形成连续闭环，而非彼此独立的模块：约束感知初始化回答如何进入潜在可行走廊，状态调度决定何时介入，CVF 候选构造给出怎样移动，保守接收判断修正是否值得保留。它们分别对应算法 \ref{alg:cvf-ae} 中的初始化、状态识别、候选生成和候选接收。

\subsubsection{约束感知初始化与参考弱引导}

CVF-AE 在随机初始化之外引入低分辨率参考控制序列，并重采样为与路径编码一致的内部控制点。该序列由平面分辨率 \SI{5}{m}、垂向分辨率 \SI{4}{m} 的三维栅格 A* 在占据、风险和边界代价上搜索得到；它仅作为 CVF-AE 的内部初始化与弱方向先验，不参与基线比较。记参考控制点为 $\{\mathbf q^{\mathrm{ref}}_k\}_{k=1}^{K}$，其向量化形式为
\begin{equation}
    \mathbf X_{\mathrm{ref}}=
    \mathrm{vec}\left(
    [(\mathbf q^{\mathrm{ref}}_1)^\mathsf T,\ldots,
    (\mathbf q^{\mathrm{ref}}_K)^\mathsf T]^\mathsf T
    \right)
    \label{eq:reference-vector}
\end{equation}

对引导个体，内部控制点在参考点附近扰动；扰动半径由局部净空和风险共同决定：
\begin{equation}
    r_k=
    \max\left(
    r_{\min},
    \frac{c_d d_{\mathrm{clr}}(\mathbf q^{\mathrm{ref}}_k)}
    {1+c_\phi \phi(\mathbf q^{\mathrm{ref}}_k)}
    \right)
    \label{eq:init-radius}
\end{equation}
\begin{equation}
    \mathbf q^{(0)}_{i,k}
    =
    \Pi_{\Omega}
    \left(
    \mathbf q^{\mathrm{ref}}_k+\boldsymbol\epsilon_{i,k}
    \right)
    \qquad
    \boldsymbol\epsilon_{i,k}\sim\mathcal U([-r_k,r_k]^3)
    \label{eq:guided-init}
\end{equation}
其中 $d_{\mathrm{clr}}(\cdot)$ 为到障碍物、禁飞区、高度边界和水平边界的最小裕度，$\phi(\cdot)$ 为风险场强度，$c_d$、$c_\phi$ 和 $r_{\min}$ 控制扰动范围。若扰动点落入硬约束内部，则先投影到边界内并局部外推；其余个体采用均匀随机初始化。对复杂障碍布局，约束感知初始化采用固定走廊控制点模板扩展初始覆盖；模板、参考序列和其余样本均按同一评价函数与 Deb 规则筛选。迭代阶段参考序列仅作为下文状态化内部候选中的弱方向项 $\mathbf d_{\mathrm{ref}}$。

约束感知初始化只能改善搜索开始时进入潜在可行走廊的机会，不能决定后续各代是否仍应施加约束引导。因此，CVF-AE 进一步依据群体的可行性、违反程度和搜索进展识别介入时机。

\subsubsection{状态识别与保守调度}

令 $\rho_f$、$\bar{V}$、$D_{\mathrm{pop}}$ 和 $\Delta J_{\mathrm{recent}}$ 分别表示可行率、平均违反量、多样性和近期目标改进：
\begin{equation}
    \rho_f=\frac{1}{N}\sum_{i=1}^{N}\mathbb I(\mathbf X_i\ \mathrm{feasible}),
    \qquad
    \bar V=\frac{1}{N}\sum_{i=1}^{N}V(\mathbf X_i)
    \label{eq:state-feasibility}
\end{equation}
\begin{equation}
    D_{\mathrm{pop}}=
    \frac{1}{3K}\sum_{j=1}^{3K}
    \mathrm{std}\left(\frac{X_{1j},\ldots,X_{Nj}}{u_j-l_j}\right)
    \label{eq:state-diversity}
\end{equation}
\begin{equation}
    \Delta J_{\mathrm{recent}}=
    \frac{\max(0,J^*_{t-w}-J^*_{t})}{\max(1,|J^*_{t-w}|)} ,
    \qquad w=15
    \label{eq:recent-improvement}
\end{equation}
当 $\Delta J_{\mathrm{recent}}<10^{-4}$ 且 $(D_{\mathrm{pop}}<0.08\lor t>2w)$ 时认为搜索停滞。设 $\chi_{\mathrm{stag}}$ 为该事件的示性函数，则保守调度在形成、保持和恢复三种状态之间切换：
\begin{equation}
    q_t=
    \begin{cases}
    \mathrm{F}, & \neg\mathrm{feasible}(\mathbf X^*)\land(\rho_f\leq0.10\lor\bar V\geq18)\\
    \mathrm{R}, & \chi_{\mathrm{stag}}=1\land\rho_f<0.55\land\bar V\geq6\\
    \mathrm{P}, & \mathrm{otherwise}
    \end{cases}
    \label{eq:conservative-state}
\end{equation}
其中 $q_t$ 为请求状态，$\mathrm{F}$、$\mathrm{P}$ 和 $\mathrm{R}$ 分别表示形成、保持和恢复。形成条件优先于恢复条件；非保持请求需连续确认 $2$ 代，确认后的生效状态记为 $s_t$。

CVF-AE 在每个生效状态下先构造内部基础候选。令 $\mathbf d_{\mathrm{ref}}=\mathbf X_{\mathrm{ref}}-\mathbf X_i$，并沿用前文的 $\mathbf d_{\mathrm{best}}$、$\mathbf d_{\mathrm{elite}}$、$\mathbf d_{\mathrm{sample}}$、$\mathbf d_{ab}$ 和 $\mathbf d_{cd}$，则
\begin{equation}
    \begin{aligned}
    \mathbf D_{\mathrm{AE}}^{(s_t)}
    ={}&c_b\mathbf r_b\odot\mathbf d_{\mathrm{best}}
    +c_e\mathbf r_e\odot\mathbf d_{\mathrm{elite}}
    +c_s\mathbf r_s\odot\mathbf d_{\mathrm{sample}}\\
    &+c_r\mathbf r_r\odot\mathbf d_{\mathrm{ref}}
    +c_1\mathbf d_{ab}+c_2\mathbf d_{cd}
    +c_u\boldsymbol\xi+c_g\boldsymbol\zeta
    \end{aligned}
    \label{eq:ae-state-direction}
\end{equation}
其中各 $\mathbf r$ 为独立均匀随机向量，$\boldsymbol\xi$、$\boldsymbol\zeta$ 分别为均匀与高斯扰动，$c_b$ 至 $c_g$ 为按状态固定的方向系数。其内部基础候选为 $\mathbf X_{\mathrm{AE}}=\Pi_{\Omega}((1-\nu_s)(\mathbf X_i+\mathbf D_{\mathrm{AE}}^{(s_t)})+\nu_s\mathbf X_{\mathrm{elite}})$，其中 $\nu_s$ 为随进程缓慢增加、在形成状态略增强的精英牵引系数。三种状态的方向侧重及 CVF 候选融合权重见表 \ref{tab:cvf-state-coefficients}；该状态化候选仅属于 CVF-AE，不属于主对比中的基础 AE。

\begin{table}[tbp]
\centering
\caption{CVF-AE 三状态的候选侧重与融合权重}
\label{tab:cvf-state-coefficients}
\footnotesize
\begin{tabularx}{\columnwidth}{@{}c X c@{}}
\toprule
$s_t$ & 内部基础方向侧重 & $(\alpha_s,\beta_s,\gamma_s)$\\
\midrule
$\mathrm{F}$ & 参考/精英方向与均匀扰动 & $(.80,.40,.04)$\\
$\mathrm{P}$ & 最优/精英方向与局部差分 & $(.90,.38,.06)$\\
$\mathrm{R}$ & 差分恢复与高斯扰动 & $(.90,.28,.04)$\\
\bottomrule
\end{tabularx}
\end{table}

\subsubsection{CVF 构建与压力分量}

CVF 不是目标或约束函数的解析梯度，而是在控制点空间中由局部约束压力构造的有界候选方向。给定低密度路径采样集 $\widetilde{\mathcal P}=\{\widetilde{\mathbf p}_m\}_{m=1}^{\widetilde M}$，采样点压力先分配到最近内部控制点：
\begin{equation}
    k^*(m)=\arg\min_{1\leq k\leq K}\|\widetilde{\mathbf p}_m-\mathbf{q}_k\|_2
    \label{eq:nearest-control}
\end{equation}
对第 $k$ 个控制点，其 CVF 方向由多个场分量叠加得到：
\begin{equation}
    \begin{aligned}
    \mathbf{F}_k =
    \sum_{c\in\mathcal{C}}
    \eta_c(s_t)\mathbf{F}_{k,c}\\
    \mathcal{C}=
    \{\mathrm{obs},\mathrm{nfz},\mathrm{risk},\mathrm{alt},\mathrm{curv},\mathrm{bnd}\}
    \end{aligned}
    \label{eq:cvf-field}
\end{equation}
其中 $\mathbf{F}_{k,c}$ 为第 $c$ 类压力方向，$\eta_c(s_t)$ 为生效状态相关权重；局部违反程度由分量幅值体现。形成状态加强障碍物、禁飞区和高度压力，保持状态采用较均衡的分量组合，恢复状态整体减弱约束牵引但保留障碍物和禁飞区方向。各状态使用固定权重，且不随测试场景调节。
若多个采样点映射到同一控制点，则按命中次数平均压力。记 $n_k$ 为映射到第 $k$ 个控制点且压力非零的采样点数量，非曲率增量为
\begin{equation}
    \Delta \mathbf q_k^{\mathrm{path}}=
    \begin{cases}
    \dfrac{1}{n_k}\sum_{m:k^*(m)=k}\mathbf F(\widetilde{\mathbf p}_m), & n_k>0\\[6pt]
    \mathbf 0, & n_k=0
    \end{cases}
    \label{eq:cvf-average-pressure}
\end{equation}
其中 $\mathbf F(\widetilde{\mathbf p}_m)$ 为采样点处各类压力的加权和。图 \ref{fig:cvf-pressure-mapping-inset} 示意了采样压力映射、平均及局部分量合成；曲率分量在控制点序列上计算，并与 $\Delta \mathbf q_k^{\mathrm{path}}$ 相加得到 $\Delta\mathbf q_k$。

\begin{figure}[tbp]
    \centering
    \includegraphics[width=\linewidth]{draft_cvf_pressure_mapping_with_synthesis_inset_preview.pdf}
    \caption{CVF 压力映射与局部分量合成示意。红色箭头为采样点压力，青绿色箭头为映射平均后的控制点增量；嵌入图展示同一数据下的分量向量合成。}
    \label{fig:cvf-pressure-mapping-inset}
\end{figure}

\paragraph{障碍物分量}
对于轴对齐障碍物 $\mathcal B=[x_1,x_2]\times[y_1,y_2]\times[z_1,z_2]$，令 $\mathbf c_{\mathcal B}(\mathbf p)$ 为 $\mathbf p$ 在盒体上的最近点。若 $\mathbf p$ 位于障碍物外部，定义
\begin{equation}
    \mathbf d_o=
    [p_x-c_x,\ p_y-c_y,\ \lambda_z(p_z-c_z)]^\mathsf T,\qquad
    d_o=\|\mathbf d_o\|_2
    \label{eq:obstacle-distance-vector}
\end{equation}
当 $0<d_o\leq r_o$ 时，障碍物压力为
\begin{equation}
    \mathbf f_{\mathrm{obs}}(\mathbf p;\mathcal B)=
    \frac{r_o-d_o}{r_o}\frac{\mathbf d_o}{d_o}
    \label{eq:obstacle-buffer-pressure}
\end{equation}
若 $\mathbf p$ 位于障碍物内部，则选择最近的水平外侧面法向 $\mathbf n_{\min}$，并令
\begin{equation}
    \mathbf f_{\mathrm{obs}}(\mathbf p;\mathcal B)=
    (r_o+d_{\min})\mathbf n_{\min}
    \label{eq:obstacle-inside-pressure}
\end{equation}
其中 $d_{\min}$ 为到最近水平面的穿透深度，$r_o$ 为障碍缓冲半径，$\lambda_z$ 为固定垂向缩放系数。

\paragraph{禁飞区分量}
设柱状禁飞区为 $\mathcal Z=(\mathbf c_z,r_z,z_l,z_u)$。若 $p_z\notin [z_l,z_u]$，该分量为零；否则令 $\mathbf d_z=[p_x-c_{z,x},p_y-c_{z,y},0]^\mathsf T$，$d_z=\|\mathbf d_z\|_2$，并定义
\begin{equation}
    \mathbf f_{\mathrm{nfz}}(\mathbf p;\mathcal Z)=
    \begin{cases}
    (r_z-d_z+r_n)\dfrac{\mathbf d_z}{d_z}, & d_z\leq r_z\\[6pt]
    \dfrac{r_z+r_n-d_z}{r_n}\dfrac{\mathbf d_z}{d_z}, & r_z<d_z\leq r_z+r_n\\[6pt]
    \mathbf 0, & d_z>r_z+r_n
    \end{cases}
    \label{eq:nfz-pressure}
\end{equation}
其中 $r_n$ 为禁飞区外侧缓冲半径；当 $d_z$ 接近零时使用固定水平单位方向避免除零。

\paragraph{风险分量}
对风险热点 $\mathcal H_h=(\mathbf c_h,\sigma_h,a_h)$，令 $\widetilde{\mathbf d}_h=[p_x-c_{h,x},p_y-c_{h,y},\lambda_z(p_z-c_{h,z})]^\mathsf T$，并定义
\begin{equation}
    \phi_h(\mathbf p)=
    a_h\exp\left(-\frac{\|\widetilde{\mathbf d}_h\|_2^2}{2\sigma_h^2}\right)
    \label{eq:risk-hotspot}
\end{equation}
仅当 $\phi_h(\mathbf p)>\phi_0$ 时触发风险压力：
\begin{equation}
    \mathbf f_{\mathrm{risk}}(\mathbf p;\mathcal H_h)=
    \kappa_r(\phi_h(\mathbf p)-\phi_0)
    \frac{\widetilde{\mathbf d}_h}{\|\widetilde{\mathbf d}_h\|_2+\epsilon_l}
    \label{eq:risk-pressure}
\end{equation}
其中 $\phi_0$ 为风险激活阈值，$\kappa_r$ 为压力强度系数。

\paragraph{高度和边界分量}
记 $z_l=z_{\min}$，$z_u=z_{\max}$，高度分量为
\begin{equation}
f_{\mathrm{alt},z}(\mathbf p)=
\begin{cases}
z_l-p_z, & p_z<z_l\\
z_u-p_z, & p_z>z_u\\
\lambda_z(z_l+b_z-p_z), & z_l\leq p_z<z_l+b_z\\
-\lambda_z(p_z-z_u+b_z), & z_u-b_z<p_z\leq z_u\\
0, & \mathrm{otherwise}
\end{cases}
\label{eq:altitude-pressure}
\end{equation}
并叠加弱参考高度项 $\lambda_{\mathrm{ref}}(z_{\mathrm{ref}}-p_z)$，其中 $b_z$ 为高度缓冲宽度，$\lambda_{\mathrm{ref}}$ 为固定弱引导系数。水平边界分量为
\begin{equation}
\begin{aligned}
f_{\mathrm{bnd},x}(\mathbf p)&=
\begin{cases}
x_{\min}+b_b-p_x, & p_x<x_{\min}+b_b\\
x_{\max}-b_b-p_x, & p_x>x_{\max}-b_b\\
0, & \mathrm{otherwise}
\end{cases}\\
f_{\mathrm{bnd},y}(\mathbf p)&\ \text{is defined analogously}
\end{aligned}
\label{eq:boundary-pressure}
\end{equation}
其中 $b_b$ 为水平边界缓冲宽度，硬边界仍由投影保证。

\paragraph{转弯分量}
若连续控制点形成的局部转角 $\vartheta_k$ 接近或超过阈值，则施加局部平滑方向。$\vartheta_k$ 按式 \eqref{eq:turning-angle} 计算，但以控制点 $\mathbf q_k$ 替代采样点 $\mathbf p_m$：
\begin{equation}
    \mathbf{F}_{k,\mathrm{curv}}=
    \begin{cases}
    \gamma_{\mathrm{curv}}
    \left[\tfrac{1}{2}(\mathbf{q}_{k-1}+\mathbf{q}_{k+1})-\mathbf{q}_k\right],
    & \vartheta_k\geq \theta_c\\[4pt]
    \mathbf 0,
    & \mathrm{otherwise}
    \end{cases}
    \label{eq:curvature-field}
\end{equation}
其中 $\gamma_{\mathrm{curv}}$ 为平滑强度，$\theta_c=\alpha_{\mathrm{curv}}\theta_{\max}$，$\alpha_{\mathrm{curv}}$ 为转弯压力的激活比例。

上述局部压力回答控制点应远离何种违反区域，但其叠加幅度不天然与路径质量一致。因此，CVF 不能直接替代基础进化候选，仍需经有界融合、稀疏触发和保守接收限制其影响范围。

\subsubsection{有界融合与稀疏接收}

将全部内部控制点增量向量化后得到未截断 CVF 步长：
\begin{equation}
    \widetilde{\mathbf D}_{\mathrm{CVF}}=
    \mathrm{vec}
    \left(
    [\Delta\mathbf q_1^\mathsf{T},\ldots,\Delta\mathbf q_K^\mathsf{T}]^\mathsf{T}
    \right)
    \label{eq:cvf-vectorization}
\end{equation}
所有分量叠加后进行逐维截断、强度缩放和整体范数截断：
\begin{equation}
    \bar D_{\mathrm{CVF},j}=
    \mathrm{clip}
    \left(
    \widetilde D_{\mathrm{CVF},j},
    -\rho_{\mathrm{step}}(u_j-l_j),
    \rho_{\mathrm{step}}(u_j-l_j)
    \right)
    \label{eq:component-clip}
\end{equation}
\begin{equation}
    \mathbf D_{\mathrm{CVF}}^{(0)}=
    \kappa_{\mathrm{cvf}}\bar{\mathbf D}_{\mathrm{CVF}}
    \label{eq:cvf-strength}
\end{equation}
\begin{equation}
    h_{\mathrm{cvf}}=\rho_{\mathrm{norm}}\|\mathbf u-\mathbf l\|_2,\qquad
    r_{\mathrm{cvf}}=\frac{h_{\mathrm{cvf}}}
    {\|\mathbf D_{\mathrm{CVF}}^{(0)}\|_2+\epsilon_l}
\end{equation}
\begin{equation}
    \mathbf D_{\mathrm{CVF}}=
    \begin{cases}
    \mathbf D_{\mathrm{CVF}}^{(0)}, &
    \|\mathbf D_{\mathrm{CVF}}^{(0)}\|_2\leq h_{\mathrm{cvf}}\\
    r_{\mathrm{cvf}}\mathbf D_{\mathrm{CVF}}^{(0)}, &
    \mathrm{otherwise}
    \end{cases}
    \label{eq:norm-clip}
\end{equation}
其中 $\kappa_{\mathrm{cvf}}$ 为 CVF 强度系数，$\rho_{\mathrm{step}}$ 和 $\rho_{\mathrm{norm}}$ 限制单次引导步长；$\mathrm{clip}(\cdot,\rho)$ 表示逐维对称截断。

质量引导方向 $\mathbf D_{\mathrm Q}$ 由当前最优解、采样精英个体和控制点平滑项共同构成：
\begin{equation}
    \begin{aligned}
    \mathbf G_{\mathrm Q}
    &={}
    \omega_{\mathrm Q}(\mathbf X^*-\mathbf X_i)
    +(1-\omega_{\mathrm Q})(\mathbf X_{\mathrm{elite}}-\mathbf X_i),\\
    \mathbf D_{\mathrm Q}
    &=\mathrm{clip}(\mathbf G_{\mathrm Q}+\mathbf D_{\mathrm{sm}},\rho_{\mathrm Q})
    \end{aligned}
    \label{eq:quality-step}
\end{equation}
其中 $\omega_{\mathrm Q}$ 为固定最优解牵引权重，$\mathbf G_{\mathrm Q}$ 为精英组合方向，$\mathbf D_{\mathrm{sm}}$ 为控制点局部平滑增量，$\rho_{\mathrm Q}$ 为逐维质量步长上界。状态融合方向和 CVF 候选为
\begin{equation}
    \begin{aligned}
    \mathbf{D}_s
    &=
    \alpha_s \mathbf{D}_{\mathrm{AE}}^{(s_t)}
    + \beta_s \mathbf{D}_{\mathrm{CVF}}
    + \gamma_s \mathbf{D}_{\mathrm{Q}}\\
    \mathbf{X}_{\mathrm{CVF}}
    &=
    \Pi_{\Omega}
    \Bigl(
    (1-\mu_t)(\mathbf{X}_i+\mathbf{D}_s)
    +\mu_t\mathbf{X}_{\mathrm{elite}}
    \Bigr)
    \end{aligned}
    \label{eq:cvf-candidate}
\end{equation}
基础候选 $\mathbf{X}_{\mathrm{AE}}$ 始终生成，CVF 候选 $\mathbf{X}_{\mathrm{CVF}}$ 仅对触发个体生成。

设第 $t$ 代状态为 $s_t$，对应触发比例为 $r_{s_t}$，则
\begin{equation}
    N_{\mathrm{cvf}}^{(t)}\leq
    \max\!\left\{1,\operatorname{round}(r_{s_t}N)\right\}
    \label{eq:cvf-quota}
\end{equation}
每代先按当前标量适应度排序，并从近可行的不可行个体中筛选触发对象。F/P 状态优先处理排序靠前的候选，R 状态放宽排序限制；三种状态的名义触发比例分别为 $r_{\mathrm F}=0.06$、$r_{\mathrm P}=0.06$ 和 $r_{\mathrm R}=0.04$。一旦种群已经产生可行最优解，则每 $3$ 代检查一次，单代比例不超过 $0.03$。其余几何缓冲、截断和局部修复筛选均作为固定实现常数，在三个场景中保持不变。

每个触发个体先评价基础候选；若生成 CVF 候选，则二者按局部规则比较：
\begin{equation}
    \mathbf{X}_{\mathrm{CVF}}
    \prec_{\mathrm{Deb}}
    \mathbf{X}_{\mathrm{AE}},
    \qquad
    J(\mathbf{X}_{\mathrm{CVF}})
    \leq
    J(\mathbf{X}_{\mathrm{AE}})
    \label{eq:local-cvf-accept}
\end{equation}
式 \eqref{eq:local-cvf-accept} 是 Deb 优先且目标不增的保守局部门控：CVF 候选必须同时满足两项条件，否则保留基础候选。稀疏可行性保持发生在候选生成之后，仅对少量候选进行碰撞、禁飞区和转弯修复，并施加局部风险降低校正。

至此，CVF-AE 将初始化先验、状态化介入、局部移动建议和候选保留边界连接为闭环；基础 AE 始终承担主体搜索，CVF 仅在必要时提供受限修正。

\subsection{算法复杂度}

设种群规模为 $N$，最大迭代次数为 $T$，完整路径评价采样点数为 $M$，CVF 低密度采样点数为 $\widetilde M$，内部控制点数为 $K$，空间约束元素数为 $G$。基础搜索的候选评价次数为 $O(TN)$，单次路径评价代价为
\begin{equation}
    O\big(M(G+K)\big)
\end{equation}

因此基础搜索的评价工作量为 $O\big(TNM(G+K)\big)$。若每代触发个体数为 $N_{\mathrm{cvf}}$，CVF 额外候选评价次数为 $O(TN_{\mathrm{cvf}})$，低密度场构造与额外完整评价的工作量为
\begin{equation}
    O\big(TN_{\mathrm{cvf}}[M+\widetilde M](G+K)\big)
\end{equation}
由于 $N_{\mathrm{cvf}}\ll N$，额外开销主要由少量触发个体承担。
